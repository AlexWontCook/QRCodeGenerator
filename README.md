Extremely basic QR code generator.

By default, it creates a new folder to store the QR codes in when you run the script. This can be changed in the GUI. 

Also generates a preview image for testing.
